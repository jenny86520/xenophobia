export enum PartyType {
  online = 'online',
  offline = 'offline',
}

export enum PartyStatus {
  draft = 'draft',
  release = 'release',
  delete = 'delete',
}

export enum SignUpStatus{
  open = 'open',
  close = 'close',
}

export enum DateType{
  fixed= 'fixed',
  pending= 'pending',
}